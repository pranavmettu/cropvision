# CropVision currently uses NASA POWER through public API calls and does not require an API key.
# Add deployment-specific environment variables here if you later deploy the Streamlit app.

STREAMLIT_SERVER_PORT=8501
